# Local Spring Boot uses 8080 by default. `.env.local` may override this target
# when a developer intentionally starts the backend on another port.
VITE_ENABLE_HOME_OVERVIEW_API=true
WBE_DEV_PROXY_TARGET=http://127.0.0.1:8080
