# Keep local startup independent from a backend. Developers can override this
# in the ignored .env.local when WBE_DEV_PROXY_TARGET or a same-origin backend
# is available.
VITE_ENABLE_HOME_OVERVIEW_API=false
